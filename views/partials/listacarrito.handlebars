<br>
<h1 class="text-center primary">Listado de productos en su carrito de compras</h1>
<br>

<div class="row justify-content-center mx-auto" style="width: 60%;">
    <table style="text-align: center;">
        <tr>
            <th>Producto</th>
            <th>Cantidad (kg)</th>
            <th>Precio ($)</th>
        </tr>
    {{# each productos }}
        <tr>
            <td>{{ this.producto }}</td>
            <td>{{ this.cantidad }}</td>
            <td>{{ this.precio }}</td>
        </tr>
    {{/ each }}
        <tr>
            <td></td>
        </tr>
        <tr>
            <td></td>
            <td style="font-weight: 500; font-size: 1.5em">Total</td>
            <td style="font-weight: 500; font-size: 1.5em">{{ total }}</td>
        </tr>
    </table>

    <button class="btn-success mt-5" id="botonPagar" style="width: 20%;">Pagar</button>
</div>