<script src="https://kit.fontawesome.com/5591d98e04.js" crossorigin="anonymous"></script>
<link href="https://fonts.googleapis.com/css2?family=Sacramento&display=swap" rel="stylesheet">

<nav class="navbar navbar-expand-lg navbar-dark bg-success">
    <div class="container-fluid">
        <a class="navbar-brand" href="/" style="font-family: Sacramento; font-size: 40px">Coma Sano, Coma Fresco SPA</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                {{!-- <li class="nav-item">
                    {{ #if usuario }}
                        <p>Hola, {{ usuario }} </p>
                    {{ else }}
                        <p>Usuario no registrado</p>
                    {{ /if }}
                </li> --}}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/mostrarcarrito">
                        {{# if comprasRealizadas }}
                            <i class="fas fa-cart-plus fa-2x mx-3"></i>
                        {{ else }}
                            <i class="fas fa-shopping-cart fa-2x mx-3"></i>
                        {{/ if}}
                    </a>
                </li>

            </ul>
        </div>
    </div>

    {{# if usuario }}
        <p class="navbar-brand"> Hola, {{ usuario }} </p>
    {{ else }}
        <p class="navbar-brand">Usuario no registrado</p>
    {{/ if }}
</nav>